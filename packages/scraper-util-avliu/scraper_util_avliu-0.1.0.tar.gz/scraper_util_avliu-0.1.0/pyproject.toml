[project]
name = "scraper_util_avliu"
version = "0.1.0"
dependencies = [
    'beautifulsoup4',
    'urllib3',
    'selenium',
    'mysql-connector',
    'undetected-chromedriver'
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
