[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"



[tool.poetry.dependencies]
python = ">=3.6,<4.0"
selenium = "^3.141.0"
requests = ">=2.30.0"
