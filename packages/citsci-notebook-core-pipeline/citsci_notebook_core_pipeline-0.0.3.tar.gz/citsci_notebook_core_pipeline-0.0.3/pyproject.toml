[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "citsci_notebook_core_pipeline"
version = "0.0.3"
dependencies = [
    "panoptes_client",
    'google.cloud.storage',
]