from setuptools import setup, find_packages

setup(
    name='greetnow',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],  # List any dependencies here
    author='fourofour',
    author_email='gorishabhio@email.com',
    description='Greet now is a simple package which provides different types of greetings',
    url='https://fourofour.org',
)
