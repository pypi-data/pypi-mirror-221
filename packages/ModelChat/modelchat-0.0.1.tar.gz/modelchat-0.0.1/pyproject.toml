[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ModelChat"
version = "0.0.1"
authors = [
  { name="Emiliano Cuellar", email="ec446162@gmail.com" },
]
description = "Una libreria para usar modelos de Chat en huggingface"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [ # Optional
  "requests"
]