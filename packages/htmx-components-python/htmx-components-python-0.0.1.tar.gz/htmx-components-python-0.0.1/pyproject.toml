[project]
name = "htmx-components-python"
authors = [
    {name = "Neil McGuigan"},
]
version = "0.0.1"
description = "HTMX Components common util for Python"
readme = "README.md"
classifiers = []
dependencies = [
    
]

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"


