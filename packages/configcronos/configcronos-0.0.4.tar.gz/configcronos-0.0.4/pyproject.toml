[project]
name = "configcronos"
version = "0.0.4"
description = "ConfigCronos is a tool to manage configuration files for multiple servers."
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "requests",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool]
