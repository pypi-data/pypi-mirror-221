[build-system]
requires = [
    "setuptools >= 64",
    "setuptools_scm >= 7",
]
build-backend = "setuptools.build_meta"

[project]
name = "dispatches-dynamic-sweep-data" 
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "dispatches-data-packages",
]

[tool.setuptools_scm]
