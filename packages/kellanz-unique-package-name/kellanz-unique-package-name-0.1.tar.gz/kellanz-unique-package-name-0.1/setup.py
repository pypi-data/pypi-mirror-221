from setuptools import setup, find_packages

setup(
    name='kellanz-unique-package-name',  # replace with your unique package name
    version='0.1',
    packages=find_packages(),
    # other metadata...
)

