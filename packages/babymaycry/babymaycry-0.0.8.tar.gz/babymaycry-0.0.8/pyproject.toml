[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "babymaycry"
version = "0.0.8"
authors = [
  { name="hvppycoding", email="hvppycoding@gmail.com" },
]
description = "Baby cry detection"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
]
dependencies = [
    "pyaudio",
    "python-telegram-bot"
]