# DESCRIPTION
#    This file contains Keystack system settings

# MANDATORY: What is the localhost Python path?
# If you are not sure, enter on cli: which python or which python3 or 
# the python command you enter to run python
# If using docker, /usr/local/bin/python3.8.
keystack_pythonPath={pythonFullPath}

keystack_dockerPythonPath={dockerPythonFullPath}

keystack_platform={platform}

# Optional: Email: Separate each email address with a comma
#           Use keystack_emailTo for production testing.
#           Use keystack_devModeEmailTo when you're developing tests
#           so emails won't get blasted to team members.
keystack_emailTo=
keystack_devModeEmailTo=
keystack_emailFrom=Keystack@Keystack

# Customize the report/email subject with any wording you like.
# You could also insert the following attributes to display their values:
#    {{datetime}} {{totalPassed}} {{totalFailed}} {{totalTestAborted}} {{totalTestcases}} 
#    {{result}} {{sessionId}} {{totalSkipped}}
# Leave	blank to use Keystack default
keystack_reportSubject=

# # Put all failure descriptions after the testcase passed/failed summary
keystack_reportPutFailureDetailsAfterResultSummary=False

# Format the report headings
# Attributes: {{pipelineId}} {{playbook}} {{startTime}} {{stopTime}} {{duration}}  {{testResultPath}}
#             {{reportHeadingAdditions}} {{totalTestcases}} {{totalPassed}} {{totalFailed}} {{totalTestAborted}}
keystack_reportHeadings=\"Playbook Executed: {{playbook}}\nSessionId: {{pipelineId}}\nTest Result Folder: {{testResultPath}}\nStartTime: {{startTime}}\nStopTime: {{stopTime}}\nDuration: {{duration}}\"

# True|False: Displays the STAGE name, MODULE name, start|stop datetime and module level results
keystack_reportIncludeStageHeadings=True

# {{testcase}} {{result}} {{aborted}} {{outerLoopCounter}} {{innerLoopCounter}
# For LoadCore & AirMosaic: {{kpiPassed}}  {{kpiFailed}}	
#    kpiPassed and kpiFailed will automatically	get excluded if	the testing module isn't LoadCore or AirMosaic
# Example: 
#keystack_reportSummary="{{testcase}}  Result:{{result}}  KPI-Passed:{{kpiPassed}}  KPI-Failed:{{kpiFailed}}  Aborted:{{aborted}}"

# Delete past results
#   0 = removes all result folders starting from today
#   1 = removes all result folders starting from yesterday
#   never = Don't remove any
#   Example=2 = Keep just the last 2 days of results and remove the rest
keystack_removeResultsFolder=5
keystack_removeLogsAfterDays=3

# Track results.
# Default=14.  Set how many days of result data to track.
# Must include param -trackResults
keystack_trackResultsForHowManyDays=14

keystack_mongoDbIp={mongoIp}
keystack_dbIpPort={mongoPort}
keystack_localHostIp={keystackIp}
keystack_keystackIpPort={keystackPort}

# For troubleshooting. Allow to switch between http and https.
# Must set above keystack_keystackIpPort correctly.
keystack_httpMethod=http



